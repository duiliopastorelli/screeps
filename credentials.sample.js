module.exports = {
    email: 'EMAIL', //opt, only private servers
    password: 'PASSWORD', //opt, only private servers
    token: '', //public server
    branch: 'default', //opt
    ptr: false, //opt, only private servers
    host: 'someprivateserver.com', //opt, only private servers
    port: 9000, //opt, only private servers
    secure: false //opt, only private servers
};